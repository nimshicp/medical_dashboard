# Frontend Notes

Use the root project README for the full setup and run guide:

- `../../README.md`

Run frontend commands from this folder:

```bash
npm install
npm run dev
```

Required env file:

```bash
cp .env.example .env
```

(Windows PowerShell)

```powershell
Copy-Item .env.example .env
```
